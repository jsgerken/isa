language: python

dist: trusty

services:
  - docker

before_script:
- chmod +x setup.sh
- ./setup.sh

script:
- docker ps
- docker-compose up
- docker network connect isa_backend mysql
- docker exec models python manage.py test
